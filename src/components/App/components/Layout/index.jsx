import React from 'react';

/**
 * @type {React.FC}
 */
const AppLayout = ({children}) => {
    return (
        <div>
            asdf
            {children}
        </div>
    );
};

export default AppLayout;